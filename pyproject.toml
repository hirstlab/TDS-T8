[tool.ruff]
target-version = "py314"
extend-exclude = ["build", "dist", ".venv"]

[tool.ruff.lint.per-file-ignores]
# MPLBACKEND, matplotlib.use('TkAgg', force=True), and hardware mocks must be installed before imports (PyInstaller requirement)
"t8_daq_system/main.py" = ["E402"]
"tests/conftest.py" = ["E402"]
